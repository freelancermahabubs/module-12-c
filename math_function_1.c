#include<stdio.h>
#include<math.h>
#include<stdlib.h>
int main(){
    // double x;
    // scanf("%lf",&x);
    // int ans = ceil(x);
    // int ans = floor(x);
    // int ans  = round(x);
    // double x;
    // scanf("%d",&x);
    // int ans = sqrt(x);
    // printf("%0.2lf\n", ans);

    // int x, y;
    // scanf("%d %d",&x, &y);
    // int ans = pow(x*1.0, y*1.0) ;
    // printf("%d\n", ans);

    int x;
    scanf("%d", &x);
    int ans= abs(x);
    printf("%d", ans);
    return 0;
}